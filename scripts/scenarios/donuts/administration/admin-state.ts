import { propRef, defaultFlag } from '../../../content-utils'

export const introductionComplete = propRef('Introduction Complete')

export const defaultStates = []

export const defaultFlags = [
    defaultFlag(introductionComplete, false),
]